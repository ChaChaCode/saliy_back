# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

enum CardStatus {
  COMING_SOON
  NEW
  NONE
  PRE_ORDER
  SALE
  SOLD_OUT
}

type Category {
  createdAt: DateTime!
  id: Int!
  isActive: Boolean!
  name: String!
  slug: String!
  type: String!
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

enum GenderType {
  FEMALE
  MALE
  UNISEX
}

"""
The `JSONObject` scalar type represents JSON objects as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSONObject

type Product {
  cardStatus: CardStatus!
  categories: [ProductCategory!]
  color: String
  createdAt: DateTime!
  description: String
  discount: Int!
  finalPrice: Float!
  gender: GenderType!
  id: Int!
  images: JSONObject!
  isActive: Boolean!
  name: String!
  price: Float!
  salesCount: Int!
  slug: String!
  stock: JSONObject!
  updatedAt: DateTime!
  viewCount: Int!
  weight: Float
}

type ProductCategory {
  category: Category!
  id: Int!
}

type ProductsResponse {
  limit: Int!
  offset: Int!
  products: [Product!]!
  total: Int!
}

type Query {
  categories: [Category!]!
  category(slug: String!): Category!
  health: String!
  newProducts(limit: Int = 20): [Product!]!
  popularProducts(limit: Int = 10): [Product!]!
  product(slug: String!): Product!
  products(categorySlug: String, gender: String, inStock: Boolean, limit: Int = 20, maxPrice: Int, minPrice: Int, offset: Int = 0, sortBy: String, sortOrder: String, status: String): ProductsResponse!
  saleProducts(limit: Int = 20): [Product!]!
  searchProducts(query: String!): [Product!]!
}